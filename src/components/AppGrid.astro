---
import { Icon } from 'astro-icon/components';
import AppCard from './AppCard.astro';
import type { AppItem } from '../data/apps';

export interface Props {
	apps: AppItem[];
	title?: string;
	subtitle?: string;
}

const { apps, title = 'Apps', subtitle = 'Kies een subpagina om direct fullscreen te openen.' } = Astro.props;
---

<section class="section" aria-label={title}>
	<div class="section-head">
		<div>
			<h2>{title}</h2>
			<p>{subtitle}</p>
		</div>
		<span class="count"><Icon name="lucide:activity" width={12} height={12} /> {apps.length} live</span>
	</div>

	<ul>
		{apps.map((app) => (
			<li>
				<AppCard app={app} />
			</li>
		))}
	</ul>
</section>

<style>
	.section {
		display: grid;
		gap: var(--space-4);
	}

	.section-head {
		display: flex;
		justify-content: space-between;
		align-items: end;
		gap: 1rem;
	}

	h2 {
		margin: 0;
		font-size: 1rem;
		letter-spacing: -0.02em;
	}

	p {
		margin: 0.25rem 0 0;
		font-size: var(--font-size-sm);
		line-height: 1.5;
		color: #94a3b8;
	}

	.count {
		display: inline-flex;
		align-items: center;
		gap: 0.35rem;
		flex: none;
		padding: var(--space-1) var(--space-3);
		border-radius: var(--radius-pill);
		font-size: var(--font-size-xs);
		text-transform: uppercase;
		letter-spacing: 0.08em;
		color: #cbd5e1;
		background: rgba(15, 23, 42, 0.7);
		border: 1px solid var(--border-subtle);
	}

	ul {
		list-style: none;
		padding: 0;
		margin: 0;
		display: grid;
		gap: var(--space-4);
	}

	@media (min-width: 48rem) {
		ul {
			grid-template-columns: repeat(2, minmax(0, 1fr));
		}
	}
</style>